<resources>
    
    <!-- Application theme. -->
    <style name="AppTheme" parent="@android:Theme.Holo.Light">
        <!-- ActionBar -->
        <item name="@android:actionBarStyle">@style/ActionBar</item>
        <item name="@android:actionOverflowButtonStyle">@style/OverFlow</item>
        <item name="@android:homeAsUpIndicator">@drawable/ic_navigation_drawer</item>
        
        <!-- Button -->
        <item name="@android:buttonStyle">@style/Button.Blue</item>
        
        <!-- Scrollbar attributes -->
        <item name="@android:scrollbarThumbVertical">@drawable/scrollbar_blue</item>
        
        <!-- List attributes -->
        <!-- item name="@android:listDivider">@drawable/list_divider</item -->
    </style>
    
    <style name="AppTheme.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    
    <!-- For activity whose main view is list, make background darker to match white list -->
    <style name="AppTheme.ListActivity">
        <item name="android:windowBackground">@color/main_background</item>
    </style>
    
    <style name="AppTheme.ListActivity.PullList">
        <item name="ptrPullText">@string/pull_load_pull</item>
        <item name="ptrRefreshingText">@string/pull_load_doing</item>
        <item name="ptrReleaseText">@string/pull_load_release</item>
        
        <item name="ptrProgressBarColor">@android:color/holo_orange_dark</item>
        <item name="ptrSmoothProgressBarStyle">@style/RefreshSmoothProgressBar</item>
    </style>
    
    <!-- Theme for start activity -->
    <style name="AppTheme.NoActionBar.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
</resources>